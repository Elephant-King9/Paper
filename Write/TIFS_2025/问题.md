# 1. 画图用什么画

ppt吗

# 2. 最终的评估数据集

师姐的是BSDS500训练 Set5、Set12、LIVE1测试

Div2k训练 SET5、SET12、KODAK241测试

# 3.baseline要多少个模型

目前RCAN、SwinIR、CATUnet、ART在A100上都跑不起来（线性变化）（Background能不能工作的尺度）

能跑起来的只有Restormer、UFormer、AST

# 4.命名问题

网络注意力部分使用ART没有改，FFN可能使用AST的FRFN没有改，也可能直接使用MLP

上下采样模块不是目前使用的

但是上采样模块还是有点简单（后续还要修改吗？）



